
  # AutoParts

  This is a code bundle for AutoParts. The original project is available at https://www.figma.com/design/AtTffGScR5EkkpLIY30Cgx/AutoParts.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  